name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Deploy
        run: |
          echo "Deployment pipeline scaffolded."
          # Real deployment steps would go here:
          # 1. Decrypt secrets with SOPS
          # 2. Push to Aptible git remote
          # 3. Wait for release
